import { App as Application } from 'vue'
import SpNFTAssets from './SpNFTAssets.vue'

import { registerComponent } from '../../utils/plugins/index'

export const Plugin = {
  install(vue: Application): void {
    registerComponent(vue, SpNFTAssets)
  },
}

export default SpNFTAssets
