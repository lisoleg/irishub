import { App as Application } from 'vue'
import SpNFTAmountSelect from './SpNFTAmountSelect.vue'

import { registerComponent } from '@starport/vue/src/utils/plugins'

export const Plugin = {
  install(vue: Application): void {
    registerComponent(vue, SpNFTAmountSelect)
  },
}

export default SpNFTAmountSelect
