import { App as Application } from 'vue'
import SpAssets from '@starport/vue/src/components/SpAssets/SpAssets.vue'

import { registerComponent } from '@starport/vue/src/utils/plugins'

export const Plugin = {
  install(vue: Application): void {
    registerComponent(vue, SpAssets)
  },
}

export default SpAssets
